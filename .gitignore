node_modules/
.DS_Store

# 大型模型文件（onnx / bin）不上传
models/
*.onnx
*.bin

# tools 里的构建产物（wasm/ruffle 等）
tools/*.wasm
tools/core.ruffle.*.js
tools/ruffle.js

# Electron 构建产物
dist/
release/*.zip
# Gateway 构建资源（太大，不推 Git，通过 npm run prepare:gateway 本地生成）
resources/targets/
dist/